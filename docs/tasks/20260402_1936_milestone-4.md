# Milestone 4: ポスター掲示ミッション追加 + 達成・XP 付与連携

## 概要

`missions.yaml` に新規ポスター掲示ミッション（slug: `poster-placement`）を追加し、地図上からポスター掲示を報告すると `achievements` + `mission_artifacts` にレコードが自動作成されて XP が付与される仕組みを実装する。

## タスク一覧

| ステップ | タスクID | Wave | 概要 |
|---------|---------|------|------|
| A | mission-yaml-and-achieve-usecase | 1 | ミッション YAML 追加 + 達成ユースケース + サービス拡張 + アクション統合 |

## タスク詳細

### ステップ A: mission-yaml-and-achieve-usecase（Wave 1）

ミッション定義を YAML に追加し、ポスター掲示報告時に achievement + mission_artifact を作成して XP を付与するユースケースを実装する。既存の `submitPosterPlacement` Server Action にミッション達成フローを統合する。

#### 作成するファイル

1. `src/features/poster-placement/use-cases/achieve-poster-placement-mission.ts`（新規）

#### 編集するファイル

2. `mission_data/missions.yaml`（ミッション追加）
3. `mission_data/category_links.yaml`（カテゴリ紐付け追加）
4. `src/features/poster-placement/services/poster-placements.ts`（関数追加）
5. `src/features/poster-placement/actions/poster-placement-actions.ts`（ミッション達成統合）

---

#### 1. missions.yaml への追加

`mission_data/missions.yaml` の `missions:` 配列の末尾に以下を追加する:

```yaml
  - slug: poster-placement
    title: ポスターを掲示しよう
    icon_url: /img/mission-icons/actionboard_icon_work_20250713_ol_mission_fallback.svg
    content: |-
      ポスターを掲示して、掲示マップから報告しましょう。
      掲示した場所と枚数を地図上のピンで報告するとXPが獲得できます。
      メニューの「ポスター掲示マップ」から報告できます。
    difficulty: 2
    required_artifact_type: TEXT
    max_achievement_count: null
    is_featured: false
    is_hidden: false
    artifact_label: ポスター掲示情報
    ogp_image_url: null
```

**設計判断**:
- `required_artifact_type: TEXT` を採用。`text_content` に掲示場所・枚数のサマリーテキストを格納する（POSTING ミッションと同じパターン）
- `max_achievement_count: null`（無制限）。ユーザーは何回でもポスター掲示を報告できる
- `difficulty: 2`（100 XP）。ポスティング（POSTING）ミッションと同等の難易度
- `is_featured: false`。XP 2 倍ボーナスなし
- `is_hidden: false`。ミッション一覧に表示する

---

#### 2. category_links.yaml への追加

`mission_data/category_links.yaml` の `local-activities` カテゴリに追加する。既存の `put-up-poster`（sort_no: 300）の直前に挿入:

```yaml
  - category_slug: local-activities
    missions:
      # ... 既存エントリ ...
      - mission_slug: posting-flyer
        sort_no: 150
      # ↓ ここに追加
      - mission_slug: poster-placement
        sort_no: 160
      # ↓ 既存
      - mission_slug: join-street-speech
        sort_no: 200
```

具体的には `posting-flyer`（sort_no: 150）の直後、`join-street-speech`（sort_no: 200）の直前に `sort_no: 160` で挿入する。

---

#### 3. ユースケース: achieve-poster-placement-mission.ts（新規）

```typescript
// src/features/poster-placement/use-cases/achieve-poster-placement-mission.ts
import type { SupabaseClient } from "@supabase/supabase-js";
import {
  calculateLevel,
  calculateMissionXp,
} from "@/features/user-level/utils/level-calculator";
import type { Database } from "@/lib/types/supabase";

/** ポスター掲示ミッションの slug */
const POSTER_PLACEMENT_MISSION_SLUG = "poster-placement";

export type AchievePosterPlacementResult =
  | {
      success: true;
      artifactId: string;
      xpGranted: number;
    }
  | {
      success: false;
      error: string;
    };

/**
 * ポスター掲示報告時にミッション達成（achievement + mission_artifact 作成 + XP 付与）を行う。
 *
 * 処理フロー:
 * 1. missions テーブルから slug でミッションを検索
 * 2. seasons テーブルから現在のアクティブシーズンを取得
 * 3. achievements テーブルにレコードを作成
 * 4. mission_artifacts テーブルにレコードを作成（text_content にサマリー）
 * 5. xp_transactions に XP トランザクションを記録
 * 6. user_levels を更新（XP 加算 + レベル再計算）
 *
 * @param adminSupabase - createAdminClient() で取得した SupabaseClient（service_role で RLS バイパス）
 * @param params.userId - 報告者のユーザー ID
 * @param params.prefecture - 都道府県（逆ジオコーディング結果。null 可）
 * @param params.city - 市区町村（逆ジオコーディング結果。null 可）
 * @param params.count - 掲示枚数
 * @returns { success: true, artifactId, xpGranted } または { success: false, error }
 */
export async function achievePosterPlacementMission(
  adminSupabase: SupabaseClient<Database>,
  params: {
    userId: string;
    prefecture: string | null;
    city: string | null;
    count: number;
  },
): Promise<AchievePosterPlacementResult> {
  const { userId, prefecture, city, count } = params;

  // 1. ミッションを slug で検索
  const { data: mission, error: missionError } = await adminSupabase
    .from("missions")
    .select("id, difficulty, is_featured, title")
    .eq("slug", POSTER_PLACEMENT_MISSION_SLUG)
    .single();

  if (missionError || !mission) {
    console.error("Poster placement mission not found:", missionError);
    return {
      success: false,
      error: "ポスター掲示ミッションが見つかりません",
    };
  }

  // 2. 現在のアクティブシーズンを取得
  const { data: season, error: seasonError } = await adminSupabase
    .from("seasons")
    .select("id")
    .eq("is_active", true)
    .single();

  if (seasonError || !season) {
    console.error("Active season not found:", seasonError);
    return {
      success: false,
      error: "アクティブなシーズンが見つかりません",
    };
  }

  // 3. achievement を作成
  const { data: achievement, error: achievementError } = await adminSupabase
    .from("achievements")
    .insert({
      user_id: userId,
      mission_id: mission.id,
      season_id: season.id,
    })
    .select("id")
    .single();

  if (achievementError || !achievement) {
    console.error("Failed to create achievement:", achievementError);
    return {
      success: false,
      error: `ミッション達成の記録に失敗しました: ${achievementError?.message ?? "unknown"}`,
    };
  }

  // 4. mission_artifact を作成
  // text_content にサマリーテキストを格納（POSTING ミッションと同じパターン）
  const locationText =
    prefecture && city ? `${prefecture}${city}` : "住所不明";
  const textContent = `ポスター掲示: ${locationText} ${count}枚`;

  const { data: artifact, error: artifactError } = await adminSupabase
    .from("mission_artifacts")
    .insert({
      achievement_id: achievement.id,
      user_id: userId,
      artifact_type: "TEXT",
      text_content: textContent,
      description: `ポスター掲示報告（${locationText}、${count}枚）`,
    })
    .select("id")
    .single();

  if (artifactError || !artifact) {
    console.error("Failed to create mission artifact:", artifactError);
    return {
      success: false,
      error: `成果物の保存に失敗しました: ${artifactError?.message ?? "unknown"}`,
    };
  }

  // 5. XP を付与
  const xpToGrant = calculateMissionXp(mission.difficulty, mission.is_featured);
  const xpDescription = `ミッション「${mission.title}」達成による経験値獲得`;

  const { error: xpTransactionError } = await adminSupabase
    .from("xp_transactions")
    .insert({
      user_id: userId,
      season_id: season.id,
      xp_amount: xpToGrant,
      source_type: "MISSION_COMPLETION",
      source_id: achievement.id,
      description: xpDescription,
    });

  if (xpTransactionError) {
    console.error("Failed to create XP transaction:", xpTransactionError);
    // XP 付与失敗はミッション達成自体の失敗にはしない（achievement は作成済み）
  }

  // 6. user_levels を更新
  let { data: currentLevel } = await adminSupabase
    .from("user_levels")
    .select("*")
    .eq("user_id", userId)
    .eq("season_id", season.id)
    .maybeSingle();

  if (!currentLevel) {
    const { data: newLevel, error: initError } = await adminSupabase
      .from("user_levels")
      .insert({
        user_id: userId,
        season_id: season.id,
        xp: 0,
        level: 1,
      })
      .select()
      .single();

    if (initError || !newLevel) {
      console.error("Failed to initialize user level:", initError);
    } else {
      currentLevel = newLevel;
    }
  }

  if (currentLevel) {
    const newXp = currentLevel.xp + xpToGrant;
    const newLevel = calculateLevel(newXp);

    await adminSupabase
      .from("user_levels")
      .update({
        xp: newXp,
        level: newLevel,
        updated_at: new Date().toISOString(),
      })
      .eq("user_id", userId)
      .eq("season_id", season.id);
  }

  return {
    success: true,
    artifactId: artifact.id,
    xpGranted: xpToGrant,
  };
}
```

#### import 先（既存コード）

- `@supabase/supabase-js` → `SupabaseClient`（型）
- `@/features/user-level/utils/level-calculator` → `calculateLevel`, `calculateMissionXp`
- `@/lib/types/supabase` → `Database`（型）

#### 実装パターン

- `src/features/mission-detail/use-cases/achieve-mission.ts` の `achieveMission` 関数と同じパターン（`adminSupabase` を引数で受け取り、achievement → artifact → XP の順で処理）
- XP 付与部分は `achieve-mission.ts` の `processXpGrant` 関数と同じロジック（xp_transactions INSERT → user_levels 取得/初期化 → XP 加算 + レベル再計算 → user_levels UPDATE）
- `text_content` のサマリーテキスト生成は POSTING ミッションの `buildPostingActivityText` と同じ発想

#### 注意事項

- **SupabaseClient を引数で受け取る**: `createAdminClient()` を内部で呼ばない（Use Case 層のルール）。呼び出し元の Action 層で `createAdminClient()` を呼んで渡す
- **max_achievement_count チェックは不要**: `null`（無制限）のため。将来的に制限を設ける場合は `achieveMission` と同じ max_achievement_count チェックロジックを追加する
- **XP 付与失敗は致命的エラーにしない**: achievement と artifact が作成済みなら成功とする（`achieveMission` と同じ方針）
- **`calculateMissionXp(difficulty: 2, is_featured: false)` = 100 XP** が付与される

---

#### 4. サービス拡張: poster-placements.ts

`src/features/poster-placement/services/poster-placements.ts` に以下の関数を **追加** する（既存関数は変更しない）:

```typescript
/**
 * ポスター掲示レコードの mission_artifact_id を更新する
 *
 * @param id - ポスター掲示レコードの ID
 * @param missionArtifactId - 紐付ける mission_artifact の ID
 */
export async function updatePosterPlacementArtifactId(
  id: string,
  missionArtifactId: string,
): Promise<void> {
  const supabase = await createAdminClient();

  const { error } = await supabase
    .from("poster_placements")
    .update({ mission_artifact_id: missionArtifactId })
    .eq("id", id);

  if (error) {
    console.error("Error updating poster placement artifact id:", error);
    throw error;
  }
}
```

#### import 先

追加の import なし（既存の `createAdminClient` を使用）。

#### 実装パターン

同ファイルの `deletePosterPlacement` と同じパターン（`createAdminClient()` → `.update()` → エラーハンドリング）。

---

#### 5. アクション統合: poster-placement-actions.ts

`src/features/poster-placement/actions/poster-placement-actions.ts` の `submitPosterPlacement` 関数を修正し、ポスター掲示レコード作成後にミッション達成フローを呼び出す。

**変更前（現在のコード）**:
```typescript
export async function submitPosterPlacement(params: {
  lat: number;
  lng: number;
  count: number;
}): Promise<{ success: true; id: string } | { success: false; error: string }> {
  try {
    const user = await requireAuth();
    const geo = await reverseGeocode(params.lat, params.lng);
    const record = await createPosterPlacement({
      user_id: user.id,
      lat: params.lat,
      lng: params.lng,
      count: params.count,
      prefecture: geo.prefecture,
      city: geo.city,
      address: geo.address,
      postcode: geo.postcode,
    });
    return { success: true, id: record.id };
  } catch (error) {
    const message =
      error instanceof Error ? error.message : "登録に失敗しました";
    return { success: false, error: message };
  }
}
```

**変更後**:
```typescript
"use server";

import type { User } from "@supabase/supabase-js";
import { reverseGeocode } from "@/features/map-posting/services/reverse-geocoding";
import { createClient } from "@/lib/supabase/client";
import { createAdminClient } from "@/lib/supabase/adminClient";  // ← 追加
import {
  createPosterPlacement,
  deletePosterPlacement,
  getPosterPlacementById,
  updatePosterPlacementArtifactId,  // ← 追加
} from "../services/poster-placements";
import { achievePosterPlacementMission } from "../use-cases/achieve-poster-placement-mission";  // ← 追加

// requireAuth は変更なし

export async function submitPosterPlacement(params: {
  lat: number;
  lng: number;
  count: number;
}): Promise<{ success: true; id: string } | { success: false; error: string }> {
  try {
    const user = await requireAuth();
    const geo = await reverseGeocode(params.lat, params.lng);

    // 1. ポスター掲示レコードを作成（既存処理）
    const record = await createPosterPlacement({
      user_id: user.id,
      lat: params.lat,
      lng: params.lng,
      count: params.count,
      prefecture: geo.prefecture,
      city: geo.city,
      address: geo.address,
      postcode: geo.postcode,
    });

    // 2. ミッション達成処理（achievement + mission_artifact + XP 付与）
    const adminSupabase = await createAdminClient();
    const missionResult = await achievePosterPlacementMission(adminSupabase, {
      userId: user.id,
      prefecture: geo.prefecture,
      city: geo.city,
      count: params.count,
    });

    // 3. poster_placements.mission_artifact_id を更新して紐付け
    if (missionResult.success) {
      try {
        await updatePosterPlacementArtifactId(
          record.id,
          missionResult.artifactId,
        );
      } catch (linkError) {
        // 紐付け失敗は致命的エラーにしない（achievement は作成済み）
        console.error(
          "Failed to link poster placement to artifact:",
          linkError,
        );
      }
    } else {
      // ミッション達成失敗はログに記録するが、ポスター掲示自体は成功とする
      console.error(
        "Failed to achieve poster placement mission:",
        missionResult.error,
      );
    }

    return { success: true, id: record.id };
  } catch (error) {
    const message =
      error instanceof Error ? error.message : "登録に失敗しました";
    return { success: false, error: message };
  }
}

// removePosterPlacement は変更なし
```

#### import 先（追加分）

- `@/lib/supabase/adminClient` → `createAdminClient`
- `../services/poster-placements` → `updatePosterPlacementArtifactId`（本タスクで追加する関数）
- `../use-cases/achieve-poster-placement-mission` → `achievePosterPlacementMission`（本タスクで作成するユースケース）

#### 実装パターン

- `src/features/map-posting/actions/posting-shapes-actions.ts` と同じパターン（Server Action 内で `createAdminClient()` を取得してユースケースに渡す）
- ミッション達成失敗をポスター掲示レコード作成の失敗にしない設計は、`achieveMission` の XP 付与失敗処理と同じ方針

#### 注意事項

- **ミッション達成失敗はポスター掲示の失敗にしない**: ポスター掲示レコードの作成が最優先。ミッション達成や紐付けが失敗しても `{ success: true }` を返す。ログには記録する
- **`createAdminClient()` の呼び出し位置**: Action 層で呼び出してユースケースに渡す（Use Case 層のルール準拠）
- **`reverseGeocode` の結果**: `prefecture` / `city` が null の場合でもミッション達成処理は実行する（text_content に「住所不明」と記録される）
- **`removePosterPlacement` は変更しない**: 削除時に achievement を取り消す（削除する）処理は本 Milestone のスコープ外。poster_placements を削除しても achievement と XP は残る設計とする
- **ミッション同期**: missions.yaml の変更は CI/CD デプロイ時に `npm run mission:sync` で DB に反映される。ローカルテスト時は手動で `pnpm run mission:sync` を実行する必要がある
