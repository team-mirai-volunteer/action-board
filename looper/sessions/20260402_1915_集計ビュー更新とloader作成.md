# 集計ビュー更新 + 型再生成 + loader 作成

- **タスクID**: city-stats-view-and-loader
- **内容**: Milestone 3 ステップ A — `poster_placement_city_stats` ビューに `avg_lat`/`avg_lng` カラムを追加し、型再生成、loader 作成

## 作成・変更したファイル
- `supabase/migrations/20260402101350_update_poster_placement_city_stats_view.sql` — ビューを DROP→再作成し avg_lat/avg_lng を追加
- `src/lib/types/supabase.ts` — `pnpm supabase gen types typescript --local` で再生成（avg_lat/avg_lng が自動追加された）
- `src/features/poster-placement/loaders/poster-placement-loaders.ts` — `fetchCityStats` Server Action（既存 getCityStats をラップ）

## 設計判断
- 既存の `poster-boards-loaders.ts` と同じ `"use server"` + service ラップパターンを踏襲
- `PosterPlacementCityStats` 型はビューの Row 型を参照しているため、型再生成のみで自動的に avg_lat/avg_lng が追加される

## 次のタスクへの申し送り
- ステップ B（city-stats-markers-ui）でこの loader と型を使って地図マーカーを描画する
