# db-migration-and-types: DBマイグレーション + 型再生成 + feature型定義

## タスク内容
Milestone 1 ステップA: `poster_placements` テーブル・RLS・集計ビューのマイグレーション作成、Supabase型再生成、feature型定義の追加。

## 作成・変更したファイル
- `supabase/migrations/20260402082833_add_poster_placements.sql` — テーブル、RLS、インデックス、集計ビュー
- `src/lib/types/supabase.ts` — `pnpm supabase gen types typescript --local` で再生成
- `src/features/poster-placement/types/poster-placement-types.ts` — Row/Insert/Update/CityStats型エクスポート

## 設計判断
- 集計ビュー `poster_placement_city_stats` は `security_invoker = true` を採用（方式A）。service層で `createAdminClient()` 経由でクエリする前提。
- 型定義は既存の `map-poster/types/poster-types.ts` パターンに準拠。

## 次のタスクへの申し送り
- ステップB（poster-placement-services）で CRUD サービスと集計クエリサービスを実装する際、`createAdminClient()` を使用すること。
- `supabase.ts` の再生成により既存コードへの影響なし（typecheck通過済み）。
