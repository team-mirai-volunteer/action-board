# server-action-and-nav: Server Action + ナビゲーションリンク追加

## タスク内容
Milestone 2 ステップ A — ポスター掲示の登録・削除 Server Action とナビゲーションリンク追加

## 作成・変更ファイル
- `src/features/poster-placement/actions/poster-placement-actions.ts` — 新規作成。submitPosterPlacement / removePosterPlacement
- `src/components/common/header-auth.tsx` — ドロップダウンに「ポスター掲示マップ」リンク追加
- `src/components/common/navbar.tsx` — モバイルメニューに「ポスター掲示マップ」リンク追加

## 設計判断
- posting-shapes-actions.ts と同じパターン（requireAuth + service 呼び出し）を踏襲
- removePosterPlacement では getPosterPlacementById で取得後に user_id を比較する認可チェックを実装（throw ではなく `{ success: false }` で返す）
- submitPosterPlacement は reverseGeocode の結果が null でもそのまま DB 保存（設計ドキュメント指示通り）

## 次のタスクへの申し送り
- ステップ B（map-page-and-components）で page.tsx やクライアントコンポーネントを作成すると `/map/poster-placement` が実際に動作する
