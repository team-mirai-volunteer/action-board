## タスク
- ID: poster-placement-services
- 内容: Milestone 1 ステップ B — CRUD サービス + 集計クエリサービスの実装

## 作成ファイル
- `src/features/poster-placement/services/poster-placements.ts` — CRUD サービス（create / getByUserId / getById / delete / getUserCount）
- `src/features/poster-placement/services/poster-placement-stats.ts` — 集計クエリサービス（getCityStats / getCityStatsByPrefecture / getTotalStats）

## 設計判断
- 設計ドキュメントの仕様をそのまま実装。既存パターン（posting-shapes.ts, poster-boards-stats.ts）に従い `import "server-only"` + `createAdminClient()` を使用
- 集計ビューは `security_invoker = true` のため `createAdminClient()`（service_role）経由で全ユーザー分を集計
- ビューの `total_count` / `placement_count` は `Number()` で変換（SUM/COUNT の結果が null になり得るため）

## 次のタスクへの申し送り
- ステップ A（DB マイグレーション + 型定義）は完了済み前提で実装
- actions 層・loaders 層の実装は次の Milestone で対応予定
